{
  "name": "alma-ai-assistant",
  "version": "1.0.0",
  "description": "ALMA - A friendly AI assistant with built-in bias detection to ensure equality, inclusion, and satisfaction",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["AI", "assistant", "bias-detection", "reality-anchors", "inclusion"],
  "author": "ALMA Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
